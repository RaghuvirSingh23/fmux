name: CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  swift-tests:
    runs-on: macos-15

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Run test suite
        run: swift test
